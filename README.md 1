# CODTECH IT Solutions Internship Projects

## 👤 Intern Information
* **Name:** Avni Goyal
* **Intern ID:** CITS9080
* **Domain:** Cyber Security & Ethical Hacking / Python Development
* **Duration:** September 8, 2026 - October 8, 2026
* **Mentor:** CodTech IT Solutions Evaluation Team

---

## 📂 Task 1: Advanced Multi-Threaded Port Scanner

### 📌 Project Overview
This tool is designed to scan a specific target IP address or domain to identify open TCP ports within the standard range (1-1024). By leveraging **Multi-threading** via Python's `concurrent.futures` module, the execution speed scales down scan times from several minutes to just a few seconds.

### 🛠️ Technology Stack
* **Language:** Python 3.x
* **Core Modules:** `socket`, `concurrent.futures`, `sys`, `datetime`

---

## 📂 Task 2: SQL Injection Vulnerability Scanner

### 📌 Project Overview
This script is a web application security utility designed to test input parameters of websites for **SQL Injection (SQLi) vulnerabilities**. It automates the process of injecting common SQL payloads (like `'` or `" OR 1=1 --`) into URLs and parses the target's HTTP responses to check for classic database error signatures.

### 🛠️ Technology Stack
* **Language:** Python 3.x
* **External Library:** `requests` (for managing HTTP GET requests)
* **Core Modules:** `sys`

---

## 📂 Task 3: Intrusion Detection System (Snort Simulation)

### 📌 Project Overview
This project is an automated network security monitoring simulation designed to function like a basic **Intrusion Detection System (IDS)** using a Snort-like signature matching engine. The script processes simulated packet data traffic in real time and inspects source IPs against a signature rule-set to trigger immediate flags and alerts for malicious behaviors like port scanning, DDoS spikes, or unauthorized login attempts.

### 🛠️ Technology Stack
* **Language:** Python 3.x
* **Core Modules:** `time`, `random`, `datetime`

### 🚀 How to Run Task 3
1. Execute the simulator file:
   ```bash
   python ids_simulator.py
   ```
2. The network interface loop will immediately boot up and begin scanning background traffic.
3. Press `Ctrl + C` at any point to stop the security monitor administration stream safely.
